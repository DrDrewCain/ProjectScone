[package]
name = "scone"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
clap = { version = "4", features = ["derive"] }
scone-core = { path = "../scone-core" }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
